本轮已完成阅读界面优化，修改文件：`apps/readest-app/src/app/foundation-spike/page.tsx`。

- 批注栏改为桌面端与正文并排的流式布局，窄屏上下排列，宽度受窗口限制，不再覆盖正文。
- 顶部工具栏默认收起，鼠标移入工具栏区域展开；新增“常驻工具栏”开关并持久化。
- 批注显示设置支持“仅下划线 / 仅小卡片 / 下划线 + 小卡片”，默认两者同时启用。
- 新增阅读进度滑杆，可拖动快速跳转，并随滚动更新百分比。
- 新增屏幕自适应布局，避免小屏幕底部内容被裁切。
- 右上角新增最小化、全屏/取消全屏、关闭窗口按钮；Tauri 桌面调用原生窗口 API，Web 环境使用浏览器能力。

验证：FoundationSpike 专项测试 9/9 通过；TypeScript `tsc --noEmit` 通过；`git diff --check` 通过。全量测试仍有仓库既有的 ZIP/CBZ/Yomitan/浏览器夹具失败（36 项），与本页面改动无关。
