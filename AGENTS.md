# Repository Agent Instructions

## Build policy

All compilation and release builds must run in GitHub Actions. Do not compile the project locally or install local build toolchains/dependencies for compilation, including Rust targets/toolchains, MSVC, MinGW, NSIS, Android SDK/NDK, or similar tooling.
